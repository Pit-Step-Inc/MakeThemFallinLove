# Redis に普通の接続（redis://…）で繋ぐときだけ要る。
#
# Vercel の Marketplace の Redis がこの形。Upstash を REST で使う場合や、
# 手元・Render でメモリに置く場合は読み込まれない（api/_lib/store.py の
# _RedisConn だけが import する）。
#
# 版を固定しているのは、デプロイのたびに中身が変わらないようにするため。
redis==5.2.1
