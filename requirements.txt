# 依存ライブラリは無い。
#
# tools/ は標準ライブラリだけで動く。Redis も Vercel Blob も
# REST API を urllib で叩いているので、パッケージを足していない
# （tools/store.py / tools/images.py 参照）。
#
# このファイルが空でも置いてあるのは、Vercel に Python の
# プロジェクトだと伝えるため。
